---
# tasks file for roles/database

- name: Install MariaDB
  dnf:
    name:
      - mariadb-server
      - mariadb
    state: present

- name: Start and enable MariaDB
  service:
    name: mariadb
    state: started
    enabled: yes

- name: Check if root password is already set
  shell: mysql -u root -e "SELECT 1" >/dev/null 2>&1
  register: root_access
  ignore_errors: yes
  changed_when: false

- name: Set MariaDB root password for initial setup
  mysql_user:
    name: root
    host: localhost
    password: "{{ mysql_root_password }}"
    login_unix_socket: /var/lib/mysql/mysql.sock
  when: root_access.rc != 0

- name: Remove anonymous users
  mysql_user:
    name: ''
    host_all: yes
    state: absent
    login_user: root
    login_password: "{{ mysql_root_password }}"

- name: Remove test database
  mysql_db:
    name: test
    state: absent
    login_user: root
    login_password: "{{ mysql_root_password }}"

- name: Ensure root uses mysql_native_password
  mysql_user:
    name: root
    host: localhost
    plugin: mysql_native_password
    password: "{{ mysql_root_password }}"
    login_user: root
    login_password: "{{ mysql_root_password }}"

- name: Allow MariaDB to connect (SELinux)
  seboolean:
    name: httpd_can_network_connect_db
    state: yes
    persistent: yes

- name: Create WordPress database
  mysql_db:
    name: "{{ wp_db_name }}"
    state: present
    login_user: root
    login_password: "{{ mysql_root_password }}"

- name: Create WordPress database user
  mysql_user:
    name: "{{ wp_db_user }}"
    password: "{{ wp_db_password }}"
    priv: '{{ wp_db_name }}.*:ALL'
    host: 192.168.221.129 #Web Server IP
    state: present
    login_user: root
    login_password: "{{ mysql_root_password }}"

- name: Open MariaDB port for web server
  firewalld:
    rich_rule: "rule family=ipv4 source address=192.168.221.129 port port=3306 protocol=tcp accept"
    zone: public
    permanent: yes
    state: enabled
    immediate: yes
  notify: Reload firewalld
